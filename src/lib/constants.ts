/**
 * Application-wide constants
 */

export const BOOK_PRICE = 145;

// TEMPORARY CHECKOUT FLOW
// Set to true to enable temporary checkout flow (manual order via email)
// Set to false to use standard Shopify checkout
// When removing this feature, delete this constant and all references to it
export const USE_TEMPORARY_CHECKOUT = true;
